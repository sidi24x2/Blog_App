const rootURL = '/api/';
const tagsUrl = rootURL + 'tags';
const articlesUrl = rootURL + 'articles';
const signupUrl = rootURL + 'users/signup';
const loginUrl = rootURL + 'users/login';
const localstorageKey = 'app_user';
const userVerifyURL = rootURL + 'users/';

export {
  rootURL,
  tagsUrl,
  articlesUrl,
  signupUrl,
  loginUrl,
  localstorageKey,
  userVerifyURL,
};
